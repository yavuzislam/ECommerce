@model ForgotPasswordDto
@{
    ViewData["Title"] = "ForgotPassword";
    Layout = "~/Views/UILayout/_UILayout.cshtml";
}

<div class="container">
    <div class="row justify-content-center mt-5">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-lg">
                <div class="card-body">
                    <h2 class="text-center mb-4">Şifrenizi Sıfırlayın</h2>
                    <p class="text-center text-muted mb-4">Lütfen şifrenizi sıfırlamak için e-posta adresinizi girin.</p>

                    <form method="post">
                        <div class="form-group mb-3">
                            <label asp-for="Email" class="form-label">E-posta Adresiniz</label>
                            <input type="email" asp-for="Email" class="form-control" placeholder="E-posta adresinizi girin" />
                            <span asp-validation-for="Email" class="text-danger"></span>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary btn-block">Şifre Sıfırlama Linki Gönder</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Özel Stil -->
<style>
    body {
        background-color: #f8f9fa;
    }

    .card {
        border: none;
        border-radius: 10px;
    }

    .btn-primary {
        background-color: #007bff;
        border: none;
        padding: 10px 20px;
        font-size: 16px;
        transition: background-color 0.3s ease;
    }

        .btn-primary:hover {
            background-color: #0056b3;
        }

    .form-control {
        padding: 10px;
        border-radius: 5px;
    }

        .form-control:focus {
            box-shadow: none;
            border-color: #007bff;
        }

    .text-center {
        color: #333;
    }

    .text-muted {
        font-size: 14px;
    }
</style>
